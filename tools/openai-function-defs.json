[
  {
    "type": "function",
    "function": {
      "name": "mdp_search_blocks",
      "description": "Keyword search across blocks. Returns matched blocks with snippets (5-char / 5-word context around each hit).",
      "parameters": {
        "type": "object",
        "properties": {
          "path": { "type": "string" },
          "query": { "type": "string" },
          "any_of": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Multi-keyword OR list."
          },
          "limit": { "type": "integer" }
        },
        "required": ["path"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mdp_list_blocks",
      "description": "Refresh block manifest. Only call if the preloaded manifest looks stale.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": { "type": "string" },
          "depth": {
            "type": "integer",
            "description": "1 = top-level only, 2 = +direct children, …"
          },
          "where": {
            "type": "object",
            "additionalProperties": { "type": "string" },
            "description": "Filter by metadata, e.g. {\"type\":\"decision\"}."
          }
        },
        "required": ["path"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mdp_get_block_meta",
      "description": "Full metadata of one block (parent, children, all metadata, no body).",
      "parameters": {
        "type": "object",
        "properties": {
          "path": { "type": "string" },
          "id": { "type": "string" }
        },
        "required": ["path", "id"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mdp_list_children",
      "description": "Direct child block ids.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": { "type": "string" },
          "id": { "type": "string" }
        },
        "required": ["path", "id"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mdp_read_block",
      "description": "Read a block's body markdown.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": { "type": "string" },
          "id": { "type": "string" },
          "include_children": {
            "type": "boolean",
            "description": "Include the subtree."
          },
          "max_lines": {
            "type": "integer",
            "description": "Cap returned lines."
          }
        },
        "required": ["path", "id"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mdp_resolve_xref",
      "description": "Resolve relationships: parent / children / superseded-by / related.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": { "type": "string" },
          "id": { "type": "string" }
        },
        "required": ["path", "id"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "mdp_tree",
      "description": "Full nested block hierarchy (metadata only).",
      "parameters": {
        "type": "object",
        "properties": {
          "path": { "type": "string" }
        },
        "required": ["path"]
      }
    }
  }
]
